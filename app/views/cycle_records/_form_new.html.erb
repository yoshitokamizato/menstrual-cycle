<%= form_with model: @cycle_record, local: true do |form| %>
  <div class="form-group cycle-date-form">
    <%= form.label :date, class: 'control-label' %>
    <%= form.text_field :date, value: @cycle_record.date, class: 'form-control bg-white', id: 'cycle-record-date-new' %>
  </div>
  <%= render 'form_shared', form: form %>

  <div class="form-group">
    <%= form.submit '記録する', class: 'btn btn-primary btn-block', id: 'cycle-record-button' %>
  </div>
<% end %>